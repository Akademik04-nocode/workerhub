# Caddy сам получает и продлевает HTTPS-сертификаты Let's Encrypt.
# Домен подставляется из переменной DOMAIN (задаётся установщиком в .env).
{$DOMAIN} {
	handle /api/* {
		reverse_proxy backend:3000
	}
	handle /health {
		reverse_proxy backend:3000
	}
	handle {
		reverse_proxy frontend:80
	}
}
